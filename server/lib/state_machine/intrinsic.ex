defmodule StateMachine.Intrinsic do
  @moduledoc """
  Intrinsic functions used to process payload template data.
  """

  defstruct [:name, :args]

  def parse(definition) do
    {:error, "TODO"}
  end

  def apply(value, ctx, args) do
    {:error, "TODO"}
  end
end
